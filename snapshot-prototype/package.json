{
  "name": "pwf-controlled-snapshot-prototype",
  "version": "0.0.0",
  "private": true,
  "description": "Self-contained, unshipped Phase 3 controlled-snapshot feasibility handoff",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "python3 -c \"compile(open('prototype_snapshot_runner.py', encoding='utf-8').read(), 'prototype_snapshot_runner.py', 'exec')\" && node --check tests/snapshot-prototype.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
